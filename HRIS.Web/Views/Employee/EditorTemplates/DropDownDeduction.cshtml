@using HRIS.Model.Sys;
@using HRIS.Service.Configuration;
@model DataReference
@{
    var data = DependencyResolver.Current.GetService<IDeductionService>().GetQuery()
        .Select(x => new DataReference()
        {
             value = x.id.Value,
             description = x.description,
        })
        .ToList();
    Html.Kendo().DropDownListFor(model => model)
        .BindTo(data)
        .OptionLabel("select")
        .DataValueField("value").DataTextField("description")
        .Render();
}
