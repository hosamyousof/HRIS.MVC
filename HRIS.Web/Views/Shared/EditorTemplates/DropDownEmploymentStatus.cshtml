@using HRIS.Model.Sys;
@using HRIS.Service.MasterFile;
@model ReferenceModel
@{
    var data = DependencyResolver.Current.GetService<IEmploymentStatusService>().GetQuery().Select(x => new ReferenceModel()
    {
        value = x.id.Value,
        description = x.description,
    }).ToList();

    Html.Kendo().DropDownListFor(model => model)
        .BindTo(data)
        .OptionLabel("select")
        .DataValueField("value").DataTextField("description")
        .Render();
}